const arr = ["learning", "js", "along", "with", "es6"];

const resultArr = arr.map((arrValue) => arrValue + "test"); //map method will create a new array with updated values it will not disturb the original array

console.log(resultArr);
console.log(arr);
